version: '3.9'

services:
  mariadb:
    container_name: mariadb
    volumes:
      - mariadb:/var/lib/mysql
    networks:
      - inception
    build:
      context: requirements/mariadb
      dockerfile: Dockerfile
    env_file: .env
    restart: on-failure

#  wordpress:
#    container_name: wordpress
#    env_file: .env
#    volumes:
#      - wordpress:/var/www/nginx
#    networks:
#      - inception
#    build:
#      context: requirements/nginx
#      dockerfile: Dockerfile
#    depends_on:
#      - mariadb
#    restart: on-failure
#
#  nginx:
#    container_name: nginx
#    volumes:
#      - wordpress:/var/www/wordpress
#    networks:
#      - inception
#    depends_on:
#      - wordpress
#    build:
#      context: requirements/nginx
#      dockerfile: Dockerfile
#    env_file: .env
#    ports:
#      - "443:443"
#    restart: on-failure

volumes:
#  wordpress:
#    driver: local
#    driver_opts:
#      type: 'none'
#      o: 'bind'
#      device: '/Users/kdi-noce/data/wordpress'
#
  mariadb:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/Users/dino/data/mariadb'

networks:
  inception:
    driver: bridge
