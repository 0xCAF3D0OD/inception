version: '3'

services:
  mariadb:
    build:
      context: ./requirements/mariadb
      dockerfile: Dockerfile
    container_name: mariadb
    env_file: .env
    networks:
      - 42-network
    restart: always

networks:
  42-network:

#services:
#  mariadb:
#    container_name: mariadb
##    volumes:
##      - mariadb:/var/lib/mysql
#    networks:
#      - inception
#    build:
#      context: requirements/mariadb
#      dockerfile: Dockerfile
#    env_file: .env
#    restart: on-failure
#
#
#networks:
#  inception:
#    driver: bridge